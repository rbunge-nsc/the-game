//javascript for rave party
var count = 1;
function playsound() {
    
}

function createRoom() {
    //insert description
    //change title
    //change header
    //change options
        //insert into text and ref of anchor (id = opt1 opt2 ... 3)
}
function delay(mi) {
    
}

class Room {
    //links is a dictionary of linkName linkRef pairs
    constructor(title, descrip, links ) {
        this.title = title;
        this.descrip = descrip;
        this.links = links;
    }
    
}